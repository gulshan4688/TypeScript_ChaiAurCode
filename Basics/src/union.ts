let subs: string | number = 45;

// this is called we can put two or more data type to a variable and assign the values 
// accordingly 

let apiResponse: 'response' | 'pending' | 'success' = 'pending'
// so u define here 3 tpyes of a variable and only equals them with these values 
apiResponse = "response";

let seatInPlane: 'side' | 'middle' | 'lower' = 'side';

seatInPlane = 'lower';


